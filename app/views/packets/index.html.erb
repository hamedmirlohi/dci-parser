<%= flash[:notice] %>

<%= form_tag packets_path, method: :get do %>
    <div>
      <%= label_tag :null_set_off0, 'null_set_off0' %>
      <%= number_field_tag :null_set_off0 %>
    </div>

    <div>
      <%= check_box_tag :sent %>
      <%= label_tag :sent, 'Sent' %>
    </div>

    <div>
      <%= check_box_tag :received %>
      <%= label_tag :received, 'Received' %>
    </div>

    <%= submit_tag 'Submit' %>
<% end %>

<ul>
  <% @packets.each do |packet| %>
    <li>
      <span><%= packet.time %></span>
    </li>
  <% end %>
</ul>
